[project]
name = "tai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "John Gorriceta", email = "johngorriceta1@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "django>=6.1",
    "sentence-transformers>=5.7.0",
]

[project.scripts]
tayai = "tai:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
